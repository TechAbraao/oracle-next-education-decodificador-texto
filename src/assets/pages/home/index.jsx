import Button from "../../components/button";

const Home = () => {
   return (
      <home className="w-full h-screen flex items-center justify-center">
         <Button buttonText={"Criptografar"}/>
      </home>
   );
};
export default Home;
